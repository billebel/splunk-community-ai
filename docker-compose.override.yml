# Development overrides - Optional RAG API and debug settings
services:
  catalyst-mcp:
    environment:
      - MCP_DEBUG=true
    healthcheck:
      start_period: 30s

  librechat:
    environment:
      - ALLOW_REGISTRATION=true
      - OPENAI_API_KEY=
    depends_on:
      - catalyst-mcp
      - mongodb